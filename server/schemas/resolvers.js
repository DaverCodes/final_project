const { A , B } = require('../models');

// const resolvers = {
//   Query: {
//     A : async () => {
//       return A .find({});
//     },
//     Bs: async (parent, { _id }) => {
//       const params = _id ? { _id } : {};
//       return B.find(params);
//     },
//   },
//   Mutation: {

// };

module.exports = resolvers;
